rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function signedIn() {
      return request.auth != null;
    }
    function isOwner(uid) {
      return signedIn() && request.auth.uid == uid;
    }
    function isFriend(uid) {
      return signedIn() && exists(/databases/$(database)/documents/users/$(uid)/friends/$(request.auth.uid));
    }
    match /users/{uid} {
      allow read: if signedIn();
      allow create, update: if isOwner(uid)
        && !request.resource.data.keys().hasAny(['height', 'heightCm', 'weight', 'weightKg', 'body']);
      allow delete: if isOwner(uid);
      match /private/{docId} {
        allow read, write: if isOwner(uid);
      }
      match /history/{logId} {
        allow read: if isOwner(uid) || isFriend(uid);
        allow create, update, delete: if isOwner(uid)
          && !request.resource.data.keys().hasAny(['height', 'heightCm', 'weight', 'weightKg', 'body']);
      }
      match /friends/{friendId} {
        allow read: if isOwner(uid) || (signedIn() && request.auth.uid == friendId);
        allow create, update, delete: if isOwner(uid);
      }
    }
    match /names/{nameId} {
      function fields() {
        return request.resource.data.keys().hasOnly(['name', 'uid', 'device'])
          && request.resource.data.name is string
          && request.resource.data.name.size() > 0
          && request.resource.data.name.size() <= 24;
      }
      function ownAccount() {
        return signedIn() && request.resource.data.uid == request.auth.uid;
      }
      allow get: if signedIn();
      allow list: if false;
      allow create: if fields() && (
        ownAccount()
        || (!signedIn() && request.resource.data.uid == null && request.resource.data.device.installId is string && request.resource.data.device.installId.size() > 0)
      );
      allow update: if fields() && (
        (signedIn() && resource.data.uid == request.auth.uid && request.resource.data.uid == request.auth.uid)
        || (signedIn() && resource.data.uid == null && ownAccount() && request.resource.data.device.installId == resource.data.device.installId)
        || (!signedIn() && resource.data.uid == null && request.resource.data.uid == null && request.resource.data.device.installId == resource.data.device.installId)
      );
      allow delete: if false;
    }
    match /challenges/{challengeId} {
      function party() {
        return signedIn() && (request.auth.uid == resource.data.from || request.auth.uid == resource.data.to);
      }
      function clean(data) {
        return !data.keys().hasAny(['height', 'heightCm', 'weight', 'weightKg', 'body']);
      }
      allow read: if party();
      allow create: if signedIn()
        && request.resource.data.from == request.auth.uid
        && request.resource.data.status == 'ban'
        && request.resource.data.banned == null
        && request.resource.data.drafted.size() == 3
        && clean(request.resource.data);
      allow update: if party()
        && request.resource.data.from == resource.data.from
        && request.resource.data.to == resource.data.to
        && request.resource.data.drafted == resource.data.drafted
        && request.resource.data.path == resource.data.path
        && request.resource.data.day == resource.data.day
        && clean(request.resource.data);
    }
  }
}
